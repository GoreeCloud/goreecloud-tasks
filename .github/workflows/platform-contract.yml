name: Platform Contract

on:
  pull_request:
    paths:
      - goreecloud.platform.yaml
      - .github/workflows/platform-contract.yml
      - scripts/tasks_production_readiness_manifest.json
      - docs/PLATFORM_CONFORMANCE.md
  push:
    branches:
      - main
    paths:
      - goreecloud.platform.yaml
      - .github/workflows/platform-contract.yml
      - scripts/tasks_production_readiness_manifest.json
      - docs/PLATFORM_CONFORMANCE.md
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: platform-contract-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    # Temporary exact pin to corrected central PR #30 until its seven-system V1.4 baseline lands on main.
    uses: GoreeCloud/GoreeCloud/.github/workflows/reusable-platform-manifest.yml@96701cc5f20c8e0deaad512d2a9f83e0411f3f18
    with:
      manifest-path: goreecloud.platform.yaml
